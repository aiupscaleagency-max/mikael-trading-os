# Quant Skills

Three local utilities, each with a standalone install prompt. The combined prompt installs all three. Python 3.10 or newer. No API keys or broker access.

Verification: `python3 -m unittest -v`.

The tools inspect data, calculate a member-specified position size and summarise a supplied journal. They do not generate trading strategies, allocate a portfolio or execute trades.
